# Thunderbird 設定は D:\git\dotfiles からシンボリックリンクで参照される。
# WSL/Windows どちらで編集しても改行差分が出ないよう LF に固定する。
config/thunderbird/** text eol=lf

# PowerShell スクリプトは Windows で実行されるため CRLF
*.ps1 text eol=crlf

# Python は LF
*.py text eol=lf
