# Windows専用ファイル（Linux/WSLでは無視）
{{- if ne .chezmoi.os "windows" }}
AppData
Documents/PowerShell
AutoHotkey
{{- end }}
# Claude Code設定はWindowsのみchezmoi管理（WSL側はNixのシンボリックリンクで管理）
{{- if ne .chezmoi.os "windows" }}
dot_claude
{{- end }}
